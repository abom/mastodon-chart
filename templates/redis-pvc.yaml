apiVersion: v1
kind: PersistentVolume
metadata:
  name: "{{ include "redis.fullname" . }}-redis"
  labels:
    backupType: {{ .Values.threefoldVdc.backup | quote }}
spec:
  capacity:
    storage: {{ .Values.redis.persistentVolume.size | quote }}
  volumeMode: Filesystem
  accessModes:
    - {{ .Values.redis.persistentVolume.accessMode | quote }}
  persistentVolumeReclaimPolicy: Recycle
  storageClassName: ""
  hostPath:
    path: {{ .Values.redis.persistentVolume.hostPath }}
    type: DirectoryOrCreate

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "redis.fullname" . }}
  labels:
    app: {{ template "name" . }}
    component: "{{ .Values.redis.name }}"
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  accessModes:
    - {{ .Values.redis.persistentVolume.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.redis.persistentVolume.size | quote }}
  storageClassName: ""
  volumeName: "{{ include "redis.fullname" . }}-redis"
